#lang racket

#|
Define a procedure that finds the maximum digit in a number.

**Acceptance criteria:**

1. All tests pass.
|#

(define (find-max n)
  (define (helper max-digit leftover)
    (cond
      [(zero? leftover) max-digit]
      [(<= (remainder leftover 10) max-digit)
       (helper max-digit (quotient leftover 10))]
      [else (helper (remainder leftover 10) (quotient leftover 10))]
      )
    )
  )

(= (find-max 55345) 5)
(= (find-max 14752) 7)
(= (find-max 329450) 9)
(= (find-max 9521) 9)
